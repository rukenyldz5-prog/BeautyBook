# Kopyalayın: copy .env.example .env  (Windows)
# .env dosyasını asla Git'e commit etmeyin.

PORT=5000
# Uzun, rastgele bir gizli anahtar kullanın (ör. openssl rand -hex 32)
JWT_SECRET=
DB_PATH=./database/beautybook.db
